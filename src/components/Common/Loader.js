import React from 'react';

import '../../styles/loader.css';

const Loader = () => {
    return (
        <div className="loader">
            <i className="fas fa-pepper-hot fa-spin fa-2x" />
        </div>
    )
};

export default Loader;